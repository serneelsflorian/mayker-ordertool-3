# Project Configuration

<!--
  Generated by /init-project. Fill in every placeholder section, then re-run /init-project to complete setup.
  Required sections are marked with [FILL IN]. Optional sections that remain placeholder are skipped by agents.
-->

## Project Mode

<!-- greenfield = new project being built from scratch; existing = established codebase being extended -->

Mode: greenfield

## Work Item Source

<!--
  tracker  = features live in ClickUp/Linear/Jira (requires issue tracker MCP)
  local    = features are markdown files under docs/issues/ (no tracker MCP needed)
  hybrid   = both tracker items and local docs/issues/ items
-->

Source: tracker

## Project Description

<!-- [FILL IN] One paragraph describing what this application does and who it is for. -->

A group food-ordering web app for a single, preselected (hardcoded) restaurant. An admin
enters the restaurant's menu, generates a shareable link, and distributes it to their team.
Anyone holding the link (a "guest" — no account, identified by a name they type) opens it,
selects menu items with optional per-item notes and quantities, and sees a running subtotal
of only their own selections. When everyone is done, the admin closes the order (final,
non-reopenable) and exports a consolidated list — grouped by item, with quantities and notes —
as copy-paste text to manually re-enter into Deliveroo. The admin can also email that overview
to a recipient (with optional CC/BCC). There is NO programmatic submission to Deliveroo. Order
state is shared across everyone with the link, keyed by an order identifier in the share URL
(e.g. `/order/:id`), and persists across page refreshes and survives across sessions/browsers.
Audience: small teams placing a shared group order. Single role (admin); everyone else is an
unauthenticated link-holder. Built as a demo.

## Tech Stack

<!--
  [FILL IN] List the languages, frameworks, and key libraries for frontend and backend.
  Example:
    Frontend: React 18 + TypeScript, Vite, Tailwind CSS
    Backend:  Node.js 20, Express, Prisma ORM, PostgreSQL 15
    Infra:    Docker Compose (local), Railway (production)
-->

Frontend: React, TypeScript, Vite, Tailwind CSS, React Router (for `/order/:id` routing)
Backend:  FastAPI, Python 3.12+, Pydantic v2, SQLAlchemy 2.x (async), Uvicorn; PostgreSQL 16; server-side transactional email (SMTP or a provider like Resend/SendGrid)
Infra:    Docker, Docker Compose (frontend, backend, Postgres)

## Test Configuration

<!--
  [FILL IN] After the scaffold feature is built this will be concrete paths. Pre-scaffold you can leave it as
  intended values, which /build-feature will verify when it creates the test infrastructure.
-->

Unit test runner:        pytest
Unit test directory:     backend/tests/unit/
Integration test runner: pytest (with httpx async test client)
Integration test dir:    backend/tests/integration/
E2E test runner:         playwright
E2E test directory:      e2e/tests/

## MCP Configuration

<!--
  List the MCP server names exactly as they appear in .mcp.json (the key under "mcpServers").
  Run `claude mcp add --scope project ...` to write .mcp.json, then update these names.
-->

Issue tracker MCP server: clickup
Git provider MCP server:  github

Issue tracker provider: clickup
Git provider:           github

<!-- [FILL IN] GitHub repository in owner/repo format -->
Repository: serneelsflorian/mayker-ordertool-3

<!-- [FILL IN] ClickUp workspace, space, folder, or list ID where features live -->
ClickUp project/list ID: workspace ID 30307190, Backlog list ID 901523929704 (space: "DEMO - Order tool")

## Feature Toggles

<!--
  Set each toggle to ENABLED or DISABLED.
  Integration Tests: run integration tests in CI (on top of unit tests)
  E2E Tests:         run Playwright E2E specs in CI
  UAT Gherkin:       generate Gherkin acceptance scenarios alongside E2E specs (validated on merge)
  Refactor Gate:     run /refactor check before each build PR is marked ready-for-review
  Security Scanning: ENABLED = block on findings; OPTIONAL = report without blocking; DISABLED = skip
-->

Integration Tests: ENABLED
E2E Tests:         ENABLED
UAT Gherkin:       OPTIONAL
Refactor Gate:     OPTIONAL
Security Scanning: OPTIONAL

## Design Reference

<!--
  NONE        = no design reference (default)
  FIGMA_MCP   = designs come from Figma via the Figma MCP (add Figma MCP server below)
  REPO_DIR    = design exports committed under design-reference/ in this repo
-->

Mode: REPO_DIR

<!-- Design exports committed under docs/prototype in this repo -->
Path: docs/prototype

<!-- Uncomment and fill in if Mode is FIGMA_MCP:
Figma MCP server:  figma
Figma file key:    [FILL IN]
-->

## Architecture Notes

<!-- Optional. Add key architectural decisions, conventions, or constraints after the scaffold feature is built. -->

Two product decisions must NOT be re-derived or guessed by agents — they are fixed:

1. **Shared, server-persisted order state keyed by URL.** Order state is shared across everyone
   holding the link, keyed by the order identifier in the share URL (`/order/:id`). It is persisted
   server-side (Postgres) and must survive page refresh and work across different browsers/sessions.
   Do NOT implement order state with browser localStorage/sessionStorage — those do not satisfy the
   cross-session sharing requirement. The frontend reads/writes state via the FastAPI backend.

2. **Export merge rule.** In the consolidated export, line items merge ONLY when both the item AND
   its note match exactly. Items with different notes remain separate lines (e.g. `3x Margherita`
   but `1x Margherita - no onions` listed separately).

Other guidance:

- **Single role.** There is exactly one privileged role, the admin. Everyone else is an
  unauthenticated link-holder who self-identifies by typing a name; there are no accounts, logins,
  or permissions for guests. Guests can only see/edit/remove their own selections, never others'.
- **Order lifecycle is one-way.** open → closed. Closing is final and cannot be reopened from the
  UI. Closed state must be enforced server-side so add/edit/remove is rejected across all sessions,
  not just hidden in the UI.
- **Restaurant is hardcoded** for the demo (not editable, no restaurant CRUD).
- **No external integrations.** Deliveroo is manual re-entry only; there is no programmatic
  submission to Deliveroo or any third-party ordering API.
- **Email is server-side and demo-only (Story 6).** The "bill will be sent shortly" message is an
  intentional, clearly light-hearted prank. Send only to addresses the admin is entitled to use,
  keep the joke unambiguous in the body, and never send email from the frontend — route it through
  the backend.
- **Suggested layout:** `backend/` (FastAPI app + tests), `frontend/` (Vite/React), `e2e/`
  (Playwright + UAT), with Docker Compose at the root wiring frontend, backend, and Postgres.

---

## Standards (always-on imports — do not remove)

@.claude/rules/coding_standards.md
@.claude/rules/user_story_alignment.md
@.claude/rules/workflow_triggers.md
@.claude/rules/mcp_integration.md
@.claude/rules/existing_codebase.md
@.claude/rules/work_items.md
